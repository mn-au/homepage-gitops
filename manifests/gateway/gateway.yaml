apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: web
  namespace: default
spec:
  gatewayClassName: envoy   # change if your class isn't 'envoy'
  listeners:
    - name: http
      protocol: HTTP
      port: 80
