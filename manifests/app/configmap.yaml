apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage
  namespace: default
  labels: { app.kubernetes.io/name: homepage }
data:
  # Minimal, known-good config
  settings.yaml: |
    title: "My GitOps Homepage"
  services.yaml: |
    []
  widgets.yaml: |
    []
  kubernetes.yaml: |
    mode: cluster
